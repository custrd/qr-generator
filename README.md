# qr-generator
Generates QR for any URL Link.
You can install the files and run it on local host with port: 3000.
Once downloaded all the files and folders, you would require to install npm modules with the following command: ```npm install express body-parser qr-image```.
